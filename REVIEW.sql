CREATE TABLE REVIEW (
    REVIEW_ID INT PRIMARY KEY,
    USER_ID INT NOT NULL,
    PROPERTY_ID INT NOT NULL,
    COMMENTS VARCHAR(1000),
    STARS FLOAT(1 , 2 ) NOT NULL,
    FOREIGN KEY (USER_ID)
        REFERENCES USERS (USER_ID),
    FOREIGN KEY (PROPERTY_ID)
        REFERENCES PROPERTY (PROPERTY_ID)
);